# ROS 2 Python packages (rclpy, std_msgs, sensor_msgs, geometry_msgs, etc.)
# NOTE: These are NOT installed via pip. They ship with ROS 2 Humble and
# become importable after you install `ros-humble-desktop` and run:
#     source /opt/ros/humble/setup.bash
# (For reference, Humble ships rclpy 3.3.x.)

# Development packages
pytest>=7.0.0  # For testing
pyyaml>=5.4.0  # For configuration
